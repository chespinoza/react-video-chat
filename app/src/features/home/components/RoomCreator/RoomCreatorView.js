import React, { Component } from 'react';
import RoomCreatorLayout from './RoomCreatorLayout';

export default class RoomCreatorView extends Component {
  render() {
    return (
      <RoomCreatorLayout />
    );
  }
}